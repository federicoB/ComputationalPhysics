The objective of this program is to define some **numerical integration** methods.

#### How to build the program
1. Create makefile from cmake with `cmake .`
2. Complie with `make`
3. Run the program with `./integrator`
